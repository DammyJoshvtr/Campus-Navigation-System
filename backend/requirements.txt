# Web framework
Flask==3.0.3
Werkzeug==3.0.3

# Database
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
SQLAlchemy==2.0.31

# PostgreSQL driver — choose ONE depending on your setup:
#   Option A (recommended, works on Windows without pg_config):
psycopg2-binary==2.9.9; sys_platform != "win32"
#   Option B (Windows) — uncomment the line below and comment out Option A:
# psycopg2==2.9.9

# MySQL driver (only needed if using MySQL instead of PostgreSQL)
# PyMySQL==1.1.1

# Authentication
Flask-JWT-Extended==4.6.0
bcrypt==4.1.3

# Email
Flask-Mail==0.10.0

# CORS
Flask-Cors==4.0.1

# Rate limiting
Flask-Limiter==3.7.0

# Environment variables
python-dotenv==1.0.1

# Production server (Linux/macOS only — not needed on Windows dev machines)
# gunicorn==22.0.0

# Testing
pytest==8.2.2
pytest-flask==1.3.0
pytest-mock==3.14.0
